const initialState = {
    data: [],
    loading: true,
    errMsg: ""
}

const characterReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default characterReducer;